Redesign reference components (dark-mode mockups) for the Performance OS page redesigns.
These .reference.txt files are layout, component, and animation references only. They are not imported or compiled by the app. Each real page is restyled to match its reference while keeping all existing data wiring, using theme tokens so both light and dark themes work.
