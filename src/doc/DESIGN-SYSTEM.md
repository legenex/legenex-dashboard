# Legenex Design System (moved)

The canonical design system now lives at the repository root: `DESIGN-SYSTEM.md`.
This file is kept only as a pointer to avoid stale links. Edit the root file, not
this one. The token and status utility classes remain in `design-system.css` in
this folder.
